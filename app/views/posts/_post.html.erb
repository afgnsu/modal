  <tr data-id="<%= post.id %>">
    <td><%= post.id  %></td>
    <td><%= post.title %></td>
    <td><%= post.content %></td>
    <td>
    <%= link_to("編輯", edit_post_path(post),remote:true ,class: "btn btn-warning") %>
    <%= link_to("刪除", post_path(post),method: :delete, remote:true, data: { confirm: '確定要刪除？' }, class: "btn btn-danger") %>
    </td>
  </tr>
