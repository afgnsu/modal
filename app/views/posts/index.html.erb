<h1>Posts with Bootstrap 4 & Modal</h1>

<table class='table table-striped'>
  <thead class='table-primary'>
    <tr>
      <th>ID</th>
      <th>標題</th>
      <th>內文</th>
      <th>操作</th>
    </tr>
  </thead>

  <tbody class="post-index" id="posts_table">

    <% @posts.each do |post| %>
      <%= render "post", :post => post  %>

    <%# @posts.each_with_index do |post,index| %>
    <!--
      <tr>
        <td><%# index+1 %></td>
        <td><%# post.title %></td>
        <td><%# post.content %></td>
        <td><%# link_to 'Show', post %>
        <%# link_to '修改', edit_post_path(post), class: 'btn btn-warning' %>
        <%# link_to '刪除', post, method: :delete, data: { confirm: '確定要刪除？' }, class: 'btn btn-danger' %></td>
      </tr> -->

    <% end %>
  </tbody>
</table>

<%= link_to("新增", new_post_path, remote: true, class:"btn btn-primary") %>

<div id="post-modal" class="modal fade"></div>
